# 此文件用于确保 Git 保留空目录
# 目录用途：资源目录，包含预构建包和编译所需源码离线压缩包
# 提醒：
#   如果你需要则需要自己按照 `README.md` 的 `获取跨平台的预编译安装包` 所述制作存放
#   创建/修改 对应符合自己需求的执行代码，逻辑可以参考 scripts/install_cling.sh 判断代码
#   创建/修改 对应符合自己需求的清理代码，可以参考 scripts/clean.sh 代码